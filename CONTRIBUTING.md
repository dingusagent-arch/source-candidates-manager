# How to contribute

1. Create a virtual environment: make venv
2. Install dev deps: make install
3. Run tests: make test

Do not commit virtualenv directories (venv/, .venv/, art-opp/venv/). Use the pre-commit hooks to avoid accidental large commits.